{
  "name": "@pm-os/reporting",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src"
  },
  "dependencies": {
    "@pm-os/ai-sdk": "workspace:*",
    "@pm-os/boards": "workspace:*",
    "@pm-os/database": "workspace:*",
    "@pm-os/integrations": "workspace:*",
    "@pm-os/shared": "workspace:*",
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
